apiVersion: metallb.io/v1beta1
kind: IPAddressPool
metadata:
  name: ip-addr-pool
  namespace: metallb-system
spec:
  addresses:
    - 172.19.0.100-172.19.0.120 # FROM docker network inspect -f '{{.IPAM.Config}}' kind
---
apiVersion: metallb.io/v1beta1
kind: L2Advertisement
metadata:
  name: empty
  namespace: metallb-system
